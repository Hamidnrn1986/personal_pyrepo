
user Tab auto-coplete 

mkdir
ls
la -la(maibg sure that .git foldere exist )
cd.. (go back to the presoivs folder)
pwd
clear
cd~ (go to the directory )
balnaslbsalsab 

git commit -m "<<commit_message>>"
git config --global use.name "someone"
git config --global user.email "hamid_n_patriot@yahoo.com"
git config --list